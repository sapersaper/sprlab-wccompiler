# Tasks: phase5-5-item-renderer

- [ ] 1. Crear `lib/codegen/item-renderer.js` con `renderItemSetup(lines, forBlock, ctx, nodeRef)`
- [ ] 2. Actualizar `lib/codegen/render-methods.js`: reemplazar llamadas a generateItemSetup/generateNestedItemSetup por renderItemSetup
- [ ] 3. Eliminar `generateItemSetup` y `generateNestedItemSetup` del codebase
- [ ] 4. Escribir tests unitarios para `renderItemSetup` en `lib/codegen/item-renderer.test.js`
- [ ] 5. `npm test` — todos los tests pasan
