{
  "name": "wcc-example",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wcc build",
    "dev": "wcc dev",
    "dev:local": "WCC_LOCAL=1 wcc dev",
    "use:local": "yarn add -D wccompiler@file:.. && echo 'Using local version'",
    "use:npm": "yarn add -D @sprlab/wccompiler && echo 'Using npm version'"
  },
  "devDependencies": {
    "@sprlab/wccompiler": "^0.0.2",
    "wccompiler": "file:.."
  },
  "volta": {
    "node": "24.0.0",
    "yarn": "4.12.0"
  }
}
