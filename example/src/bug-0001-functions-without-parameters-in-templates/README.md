# Test Case 1: Function WITHOUT Collision (Should Work)

This component tests that functions without parameters work correctly when there's NO name collision with signals.

## Expected Behavior
- Function `getGreeting()` should be callable in template
- Should display "Hello World"
- No compilation errors
